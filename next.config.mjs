/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/NextJS-Landing-Page",
  output: "export",
  images: {
    domains: ["images.unsplash.com"],
  },
};

export default nextConfig;
